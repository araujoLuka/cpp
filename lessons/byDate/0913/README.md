Destructor has declared by this way:

```cpp
# Disciplina.hpp
~Disciplina();
```

But it need to be implemented in cpp file from the class

That's a bit hard to do, 'cause it need to clean all the stash that the class has produced
